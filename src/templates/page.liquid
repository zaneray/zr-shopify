<div class="container">
  <div class="row">
    <div class="text-column">

      <h1>{{ page.title }}</h1>

      <div class="rte">
        {{ page.content }}
      </div>
      </div>
    </div>
  </div>